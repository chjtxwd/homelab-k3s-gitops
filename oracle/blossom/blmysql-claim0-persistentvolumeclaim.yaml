apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: blmysql-claim0
  name: blmysql-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
