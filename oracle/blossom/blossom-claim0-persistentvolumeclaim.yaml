apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: blossom-claim0
  name: blossom-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
