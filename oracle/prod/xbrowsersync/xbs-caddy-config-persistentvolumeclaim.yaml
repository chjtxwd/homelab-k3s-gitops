apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: xbs-caddy-config
  name: xbs-caddy-config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
