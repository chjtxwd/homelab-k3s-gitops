apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: xbs-db-data
  name: xbs-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
