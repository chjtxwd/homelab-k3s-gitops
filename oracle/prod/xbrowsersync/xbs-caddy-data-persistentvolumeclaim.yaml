apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: xbs-caddy-data
  name: xbs-caddy-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
