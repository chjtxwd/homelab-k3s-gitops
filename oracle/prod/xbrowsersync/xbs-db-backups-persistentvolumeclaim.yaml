apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: xbs-db-backups
  name: xbs-db-backups
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
